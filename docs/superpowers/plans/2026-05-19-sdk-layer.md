# SDK Layer Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Extract high-frequency state/data operations from SKILL.md into Python scripts.

**Architecture:** Two standalone CLI scripts (`teach-state.py`, `teach-data.py`) under `scripts/`, called by the agent during teaching. Pure Python stdlib, zero pip dependencies.

**Tech Stack:** Python 3.10+, json, pathlib, argparse, sys, time

**Design Doc:** `docs/superpowers/specs/2026-05-19-sdk-layer-design.md`

---

## File Structure

```
.opencode/skills/teach-anything/
├── SKILL.md                          ← Modify: replace raw state/data ops with script calls
└── scripts/
    ├── teach-state.py                ← Create: checkpoint management
    └── teach-data.py                 ← Create: data persistence

AGENTS.md                             ← Modify: add scripts/ to project structure
```

---

### Task 1: Create `teach-state.py`

**Files:**
- Create: `.opencode/skills/teach-anything/scripts/teach-state.py`

- [ ] **Step 1: Write `teach-state.py` with argparse dispatch and 4 commands**

```python
#!/usr/bin/env python3
"""teach-anything 检查点管理。

高频操作：init → step(每步1次) → next(每模块1次) → get(恢复时)
"""
import argparse
import json
import sys
from pathlib import Path


def cmd_init(args):
    path = Path(args.path) / ".teaching-state.json"
    if path.exists() and not args.force:
        print("检查点文件已存在，使用 'step' 继续或手动删除后重新初始化", file=sys.stderr)
        sys.exit(4)
    modules = [m.strip() for m in args.modules.split(",")]
    state = {
        "topic": args.topic,
        "plan": [{"name": m, "completed": False, "stepsDone": 0} for m in modules],
        "currentModuleIndex": 0,
        "currentStep": 0,
    }
    path.parent.mkdir(parents=True, exist_ok=True)
    path.write_text(json.dumps(state, ensure_ascii=False, indent=2))
    print(f"检查点已初始化: {path}")


def cmd_step(args):
    path = Path(args.path) / ".teaching-state.json"
    if not path.exists():
        print("未找到检查点", file=sys.stderr)
        sys.exit(1)
    step = args.step
    if step < 1 or step > 7:
        print("step_number 必须在 1-7 之间", file=sys.stderr)
        sys.exit(2)
    try:
        raw = path.read_text()
        state = json.loads(raw)
    except json.JSONDecodeError:
        print("检查点文件损坏，请手动检查", file=sys.stderr)
        sys.exit(3)
    idx = state["currentModuleIndex"]
    state["currentStep"] = step
    state["plan"][idx]["stepsDone"] = step
    if step == 7:
        state["plan"][idx]["completed"] = True
    path.write_text(json.dumps(state, ensure_ascii=False, indent=2))


def cmd_next(args):
    path = Path(args.path) / ".teaching-state.json"
    if not path.exists():
        print("未找到检查点", file=sys.stderr)
        sys.exit(1)
    try:
        state = json.loads(path.read_text())
    except json.JSONDecodeError:
        print("检查点文件损坏，请手动检查", file=sys.stderr)
        sys.exit(3)
    if state["currentModuleIndex"] >= len(state["plan"]) - 1:
        print("已是最后一个模块，教学已完成，使用 'teach-data.py save' 保存记录", file=sys.stderr)
        sys.exit(2)
    state["currentModuleIndex"] += 1
    state["currentStep"] = 0
    path.write_text(json.dumps(state, ensure_ascii=False, indent=2))


def cmd_get(args):
    path = Path(args.path) / ".teaching-state.json"
    if not path.exists():
        print("未找到检查点", file=sys.stderr)
        sys.exit(1)
    try:
        state = json.loads(path.read_text())
    except json.JSONDecodeError:
        print("检查点文件损坏，请手动检查", file=sys.stderr)
        sys.exit(3)
    print(json.dumps(state, ensure_ascii=False))


def main():
    parser = argparse.ArgumentParser(
        description="teach-anything 检查点管理",
        formatter_class=argparse.RawDescriptionHelpFormatter,
    )
    sub = parser.add_subparsers(dest="command", required=True)

    p = sub.add_parser("init", help="初始化检查点")
    p.add_argument("path", help="learn-<topic>/ 目录路径")
    p.add_argument("--topic", required=True)
    p.add_argument("--modules", required=True, help="逗号分隔的模块名")
    p.add_argument("--force", action="store_true", help="覆盖已有检查点")

    p = sub.add_parser("step", help="推进步骤（高频，每步1次）")
    p.add_argument("path")
    p.add_argument("step", type=int, help="步骤号 1-7")

    p = sub.add_parser("next", help="切换到下一模块（低频，每模块1次）")
    p.add_argument("path")

    p = sub.add_parser("get", help="读取检查点（JSON stdout）")
    p.add_argument("path")

    args = parser.parse_args()

    if args.command == "init":
        cmd_init(args)
    elif args.command == "step":
        cmd_step(args)
    elif args.command == "next":
        cmd_next(args)
    elif args.command == "get":
        cmd_get(args)


if __name__ == "__main__":
    main()
```

- [ ] **Step 2: Test the script manually**

```bash
cd /mnt/c/Users/k/Desktop/WorkSpace-CC/projects/interactive-learning-lab

# Test init
python3 .opencode/skills/teach-anything/scripts/teach-state.py init /tmp/test-teach \
  --topic "Python" --modules "变量与类型,控制流"

# Verify file created
cat /tmp/test-teach/.teaching-state.json

# Test step
python3 .opencode/skills/teach-anything/scripts/teach-state.py step /tmp/test-teach 3

# Verify step updated
python3 .opencode/skills/teach-anything/scripts/teach-state.py get /tmp/test-teach

# Test next
python3 .opencode/skills/teach-anything/scripts/teach-state.py next /tmp/test-teach

# Verify module advanced
python3 .opencode/skills/teach-anything/scripts/teach-state.py get /tmp/test-teach

# Test step out of range -> exit 2
python3 .opencode/skills/teach-anything/scripts/teach-state.py step /tmp/test-teach 0
echo "Exit: $?"

# Test init on existing -> exit 4
python3 .opencode/skills/teach-anything/scripts/teach-state.py init /tmp/test-teach \
  --topic "Go" --modules "语法"
echo "Exit: $?"

# Cleanup
rm -rf /tmp/test-teach
```

Expected: All commands produce correct JSON output; error cases return correct exit codes.

- [ ] **Step 3: Commit**

```bash
git add .opencode/skills/teach-anything/scripts/teach-state.py
git commit -m "feat: teach-state.py checkpoint management script"
```

---

### Task 2: Create `teach-data.py`

**Files:**
- Create: `.opencode/skills/teach-anything/scripts/teach-data.py`

- [ ] **Step 1: Write `teach-data.py` with save command (stdin + mtime concurrency)**

```python
#!/usr/bin/env python3
"""teach-anything 会话数据持久化。

将完整 session 记录通过 stdin 原子追加到 dashboard-data.json。
"""
import argparse
import json
import sys
import time
from pathlib import Path

DATA_FILE = Path("dashboard-data.json")
MAX_RETRIES = 3
RETRY_DELAY = 0.1


def cmd_save():
    raw = sys.stdin.read()
    if not raw:
        print("缺少输入：通过 stdin 传入 session JSON", file=sys.stderr)
        sys.exit(1)

    try:
        record = json.loads(raw)
    except json.JSONDecodeError as e:
        print(f"JSON 格式无效: {e}", file=sys.stderr)
        sys.exit(2)

    required = ("topic", "date", "modules")
    missing = [f for f in required if f not in record]
    if missing:
        print(f"缺少必填字段: {', '.join(missing)}", file=sys.stderr)
        sys.exit(2)

    if DATA_FILE.exists():
        for attempt in range(MAX_RETRIES):
            mtime = DATA_FILE.stat().st_mtime_ns
            try:
                data = json.loads(DATA_FILE.read_text())
            except json.JSONDecodeError:
                data = {"sessions": []}
            data.setdefault("sessions", []).append(record)
            DATA_FILE.write_text(json.dumps(data, ensure_ascii=False, indent=2))
            if DATA_FILE.stat().st_mtime_ns == mtime:
                break  # 写入期间无其他进程修改
            time.sleep(RETRY_DELAY)
        else:
            print("写入 dashboard-data.json 失败：重试超限，请手动检查", file=sys.stderr)
            sys.exit(3)
    else:
        data = {"sessions": [record]}
        DATA_FILE.write_text(json.dumps(data, ensure_ascii=False, indent=2))

    print("学习记录已保存，打开 dashboard.html 查看学习仪表盘。")


def main():
    parser = argparse.ArgumentParser(description="teach-anything 数据持久化")
    parser.add_argument("command", nargs="?", default="save",
                        help="命令（当前仅支持 save）")
    args = parser.parse_args()
    if args.command == "save" or args.command is None:
        cmd_save()
    else:
        print(f"未知命令: {args.command}", file=sys.stderr)
        sys.exit(2)


if __name__ == "__main__":
    main()
```

- [ ] **Step 2: Test the script manually**

```bash
cd /mnt/c/Users/k/Desktop/WorkSpace-CC/projects/interactive-learning-lab

# Test save with valid record
echo '{"topic":"Python","date":"2026-05-19","modules":[{"name":"变量与类型","completed":true}],"errors":[]}' | \
  python3 .opencode/skills/teach-anything/scripts/teach-data.py save

# Verify written
cat dashboard-data.json

# Test save again (append)
echo '{"topic":"Docker","date":"2026-05-19","modules":[{"name":"Dockerfile","completed":false}],"errors":[]}' | \
  python3 .opencode/skills/teach-anything/scripts/teach-data.py save

# Verify appended (2 sessions)
python3 -c "import json; d=json.load(open('dashboard-data.json')); print(len(d['sessions']))"

# Test missing field -> exit 2
echo '{"topic":"test"}' | \
  python3 .opencode/skills/teach-anything/scripts/teach-data.py save
echo "Exit: $?"

# Test empty stdin -> exit 1
echo "" | \
  python3 .opencode/skills/teach-anything/scripts/teach-data.py save
echo "Exit: $?"

# Test invalid JSON -> exit 2
echo "not-json" | \
  python3 .opencode/skills/teach-anything/scripts/teach-data.py save
echo "Exit: $?"
```

Expected: Records appended correctly; error cases return correct exit codes.

- [ ] **Step 3: Restore dashboard-data.json to clean state (if corrupted)**

```bash
cd /mnt/c/Users/k/Desktop/WorkSpace-CC/projects/interactive-learning-lab
echo '{"sessions":[]}' > dashboard-data.json
```

- [ ] **Step 4: Commit**

```bash
git add .opencode/skills/teach-anything/scripts/teach-data.py
git commit -m "feat: teach-data.py session data persistence script"
```

---

### Task 3: Update SKILL.md

**Files:**
- Modify: `.opencode/skills/teach-anything/SKILL.md`

Replace 3 sections in SKILL.md:

- [ ] **Step 1: Update `compatibility` metadata**

Old:
```
compatibility: 独立 skill，不依赖外部文件
```

New:
```
compatibility: 独立 skill，依赖 Python 3.10+ 标准库（scripts/ 目录下）
```

- [ ] **Step 2: Replace raw checkpoint operations in "教学环境管理" section**

In the `init` call when creating a new teaching session, replace the manual Write with:

```
# 在方案确认后、第1模块进入前
python3 ${SKILL_DIR}/scripts/teach-state.py init learn-<topic> \
  --topic "<主题>" --modules "<模块1>,<模块2>,..."
```

In the "进度持久化" line, replace:
```
每步完成后写入 `learn-<topic>/.teaching-state.json` 检查点文件
```
with:
```
每步完成后执行 `teach-state.py step learn-<topic> <步骤号>` 更新检查点
```

- [ ] **Step 3: Replace "步骤推进" mentions in 7-step flow**

In the intro to Phase 2 (before step 1), add a script reference:

```
每步完成后执行（项目根目录）：
python3 .opencode/skills/teach-anything/scripts/teach-state.py step learn-<topic> N

模块切换时执行：
python3 .opencode/skills/teach-anything/scripts/teach-state.py next learn-<topic>
```

- [ ] **Step 4: Replace "检查点恢复" section**

Replace the manual read-display-resume flow with:

```
用户说"继续学习"或"继续"时：

1. 执行（项目根目录）：
   python3 .opencode/skills/teach-anything/scripts/teach-state.py get learn-<topic>
   若 exit code 非 0（无检查点），按正常 Phase 1 流程协商新方案

2. 解析 stdout JSON，展示进度："上次学到模块 N「名称」的第 M 步，继续还是先回顾一下？"

3. 用户确认后，继续执行第 M 步教学
```

- [ ] **Step 5: Replace "会话数据持久化" section**

Replace the entire section with:

```
# 会话数据持久化

教学会话结束时（全部模块完成或用户退出），执行：

```bash
# 1. 先由 Agent 构建完整 session 记录并写入临时文件
cat > /tmp/session-record.json << 'EOF'
{
  "topic": "<主题>",
  "date": "<YYYY-MM-DD>",
  "modules": [...],
  "errors": [...]
}
EOF

# 2. 通过 teach-data.py 原子追加
cat /tmp/session-record.json | python3 .opencode/skills/teach-anything/scripts/teach-data.py save
```

**规则**：
- 仅当有实质性教学交互时才写入（至少完成 1 个教学步骤）
- 模块 `completed` 仅在完成该模块全部 7 步后标记为 `true`
- 日期格式使用 `YYYY-MM-DD`（本地日期，不含时区）
- 错误记录来自 RCA 引导协议中的"归档学习"数据
- 写入后提示用户："学习记录已保存，打开 dashboard.html 查看学习仪表盘。"
- **并发安全**：teach-data.py 内置 mtime 校验 + 重试机制，Agent 无需额外处理
```

- [ ] **Step 6: Commit**

```bash
git add .opencode/skills/teach-anything/SKILL.md
git commit -m "feat: integrate teach-state.py and teach-data.py into SKILL.md"
```

---

### Task 4: Update AGENTS.md

**Files:**
- Modify: `AGENTS.md`

- [ ] **Step 1: Add `scripts/` to project structure**

In the "项目结构" section, update:

```
├── .opencode/skills/
│   └── teach-anything/
│       ├── SKILL.md            教学 skill（规范源）
│       └── scripts/            教学工具脚本
│           ├── teach-state.py   检查点管理
│           └── teach-data.py    数据持久化
```

- [ ] **Step 2: Commit**

```bash
git add AGENTS.md
git commit -m "docs: add scripts/ directory to project structure"
```

---

### Task 5: Sync to `.claude/skills/`

- [ ] **Step 1: Copy scripts directory**

```bash
cp -r .opencode/skills/teach-anything .claude/skills/teach-anything
```

- [ ] **Step 2: Commit**

```bash
git add .claude/skills/teach-anything/scripts/
git commit -m "chore: sync teach-anything scripts to .claude/skills/"
```

---

## Self-Review Checklist

- [x] Spec coverage: teach-state.py (init/step/next/get), teach-data.py (save) all covered
- [x] No TBD/TODO/placeholder content in plan
- [x] Type consistency: all script paths use `.opencode/skills/teach-anything/scripts/`
- [x] All error codes match spec (1=no file, 2=bad arg, 3=corrupt, 4=init exists)
- [x] teach-data.py concurrency logic matches spec (mtime check + retry)
- [x] AGENTS.md project structure updated with scripts/
- [x] .claude/skills/ sync step included
