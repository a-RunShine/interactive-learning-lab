schema: spec-driven

context: |
  Tech stack: POSIX Shell (scripts), Markdown (docs/teaching content)
  We use conventional commits (feat:, fix:, docs:, chore:)
  Domain: AI-agent-driven Git interactive teaching
  Primary audience: Chinese-speaking zero-basis learners
  Teaching protocol: 7-step flow defined in TEACHING_PROTOCOL.md
  Module dependency: strict sequential M0→M1→M2→M3→M4→M5 (M6 optional)
  Agent guidance: AGENTS.md (OpenCode/Codex), CLAUDE.md (Claude Code)
  Verification: scripts/verify/module-N.sh per module
  Branch naming: main (not master)

rules:
  proposal:
    - Keep proposals in Chinese
    - Use kebab-case for capability names
  specs:
    - Capabilities must have at least one scenario per requirement
    - Use SHALL/MUST for normative requirements
  tasks:
    - Each task must be a checkbox (- [ ])
    - Group tasks under ## numbered headings
    - Tasks should reference module numbers for context
